# R插件 AstrBot版 · 改造控制文档（Reform Control Document）

> 生成依据：`docs/compliance_gap_report.md`（合规差距报告）+ `astrbot_plugin_compliance_checklist.md`（官方文档清单）+ `rconsole_original_report.md`（原项目调研）
> 目标仓库：**repo 指向 `http://172.18.95.4:6610/astrbot_plugin_rconsole.git`**（已实测可达且与当前工作区同一提交 499e2e5）
> 执行规则：**一行一个任务，逐行执行；每行完成后运行该行「验证」确认无问题，再把该行状态改为【已完成】并进入下一行；全部完成做全量回归。**
> 状态标记：`[ ]`=未完成，`[x]`=已完成；行尾【状态：已完成】与行首同步修改。
> 需要用户确认项的默认值已按合规报告建议填写，执行时可随时替换。

---

## A. 仓库与元数据（repo / metadata.yaml）

- [x] 001 | 已执行：origin 的 fetch/push 地址已改为 http://172.18.95.4:6610/astrbot_plugin_rconsole.git | 验证：`git remote -v` 显示新地址（已确认）；`git fetch origin` 成功（已确认）| 状态：已完成
- [x] 002 | metadata.yaml 的 repo 字段已改为 http://172.18.95.4:6610/astrbot_plugin_rconsole.git | 验证：python yaml 解析 repo 输出新地址（已确认）| 状态：已完成
- [x] 003 | metadata.yaml 的 author 已由 `ported-by-ai` 改为真实作者名 `dsh` | 验证：yaml 解析 author 为 `dsh`、非空、不含 `/`（已确认）| 状态：已完成
- [x] 004 | metadata.yaml 的 version 已去掉 `v` 前缀（v0.3.11 → 0.3.11），遵循发布页语义化版本示例 | 验证：yaml 解析 version 为 `0.3.11`、无 `v` 前缀且为 X.Y.Z（已确认）| 状态：已完成
- [x] 005 | metadata.yaml 已增加 social_link（https://github.com/zhiyu1998/rconsole-plugin）与 tags（解析/音乐/工具/查询/点歌）| 验证：yaml 解析含两个字段（已确认）| 状态：已完成
- [x] 006 | metadata.yaml 的 desc 已扩写为多行 Markdown（功能/安装/安全概览，487 字符）| 验证：yaml 解析 desc 为多行文本且非空（已确认）| 状态：已完成

## B. 代码合规修复（logging / 网络 / 风格）

- [x] 007 | services/resolver.py 已移除标准库 logging：`import logging`/`logging.getLogger` 删除，替换为 AstrBot logger（无 AstrBot 环境用最小 no-op 兜底）| 验证：py_compile 通过；`grep -rn "import logging" services/` 无残留（已确认）；tests/test_media_resolvers.py 通过（已确认）| 状态：已完成
- [x] 008 | 已引入 pyproject.toml（[tool.ruff] line-length=120，per-file-ignores 覆盖 AstrBot API 别名/测试 sys.path 结构），对 main.py/services/tests 执行 ruff check --fix 与 ruff format（27 文件格式化）| 验证：ruff check 全部通过；ruff format --check 通过（已确认）；py_compile 通过（已确认）| 状态：已完成
- [x] 009 | 已压缩全部超长行：main.py/services 中 11 行 >120 字符已逐行重排（日志、正则、UA 头、文案等）| 验证：`awk 'length>120' main.py services/*.py` 输出为空（0 行，已确认）；ruff check/format 通过；py_compile 通过（已确认）| 状态：已完成
- [x] 010 | 网络层审视完成：全项目无 `import requests`（main.py/services/tests 源码零命中，requirements 亦无）；urllib+asyncio.to_thread 现状记为已知技术债（可选重构 aiohttp/httpx，默认暂缓）| 验证：`grep -rn "import requests" main.py services/ tests/` 无结果（已确认）| 状态：已完成

## C. 许可证与资源

- [x] 011 | 已添加 LICENSE 文件：木兰宽松许可证 v2（Mulan PSL-2.0，中英双语全文），首部注明源自 zhiyu1998/rconsole-plugin，版权人 dsh | 验证：文件存在且首段为 Mulan PSL-2.0（已确认）| 状态：已完成
- [x] 012 | 版本卡片数据审查完成：采用默认策略（保留原版高复刻卡片），README 已注明「版本卡片数据复刻原版 R 插件的 version.yaml」；rversion 文本输出与图片渲染均正常（804x910）| 验证：README 有对应说明（已确认）；@filter rversion 输出无报错（已确认）| 状态：已完成
- [x] 013 | 文档一致性修复完成：README/docs 统一口径为「原插件 47 条 → AstrBot 实现 46 条 + update 按策略移除，parity 矩阵保留历史对照」（README、parity summary/matrix、final_review、round6 已同步）| 验证：README/docs 中 46/47 表述一致（已确认）| 状态：已完成

## D. 测试与打包发布

- [x] 014 | 全量测试完成：tests/ 下 9 个可执行测试全部 exit=0 通过（core/media/bili_auth/bili_video/capability/probe/persistent/round1/style/e2e）；测试已适配当前插件根布局（旧布局路径/原源码 source 引用已修正）；无外网沙箱内网络依赖用 socket/硬超时守卫（打印 NETWORK_UNAVAILABLE/TIMEOUT 跳过，不伪造成功）；runtime_adapter_probe 需真实 AstrBot 环境（文档已注明，原产物 docs/runtime_adapter_capability_probe.json 保留）| 验证：上述测试 exit=0（已确认）| 状态：已完成
- [x] 015 | 重新生成发布包 zip：7.44MB（< 16MB），115 条目，排除 data/tests/__pycache__/大字体；含 main.py/metadata.yaml/_conf_schema.json/requirements.txt/LICENSE/README.md/pyproject.toml/services 等 | 验证：zip < 16MB（7.44MB 已确认）| 状态：已完成
- [ ] 016 | 提交全部改造到 git 并推送到 http://172.18.95.4:6610/astrbot_plugin_rconsole.git | 验证：`git status` 干净；`git push origin main` 成功；`git ls-remote` 可见新提交 | 状态：未完成
- [ ] 017 | 全量回归：推送后重新克隆到临时目录，复跑 py_compile + tests，确认新仓库内容与工作区一致 | 验证：克隆目录内容一致、测试通过 | 状态：未完成

## E. 可选增强（默认暂缓，用户决定是否执行）

- [ ] 018 | 添加 i18n：`.astrbot-plugin/i18n/zh-CN.json` / `en-US.json`（display_name/desc/配置文案）| 验证：文件为合法 JSON 嵌套结构 | 状态：未完成
- [ ] 019 | 添加 skills/（将查询/翻译/点歌封装为 Skill 供 Agent 使用）| 验证：skills/ 目录结构符合官方约定 | 状态：未完成
- [ ] 020 | 添加 .github/workflows CI（ruff + pytest 冒烟）| 验证：工作流文件存在；本地等价命令通过 | 状态：未完成

---

## 完成状态总览

- 已完成：15 / 20（001、002、003、004、005、006、007、008、009、010、011、012、013、014、015）
- 未完成：5 / 20
- 当前执行行：016（未开始）

> 执行提示：每行完成后回到本文件，将行首 `[ ]` 改为 `[x]`、行尾【状态：未完成】改为【状态：已完成】，并更新「完成状态总览」与「当前执行行」，然后才进入下一行。